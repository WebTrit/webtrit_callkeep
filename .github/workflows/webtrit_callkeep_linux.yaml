name: webtrit_callkeep_linux

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

# Linux is a stub platform — CI disabled until actively supported.
# To re-enable: restore the pull_request and push triggers below.
on:
  workflow_dispatch:

jobs:
  build:
    uses: VeryGoodOpenSource/very_good_workflows/.github/workflows/flutter_package.yml@v1
    with:
      flutter_channel: stable
      flutter_version: 3.32.0
      working_directory: webtrit_callkeep_linux
      format_line_length: "120"

